import { calcHandicap } from 'golf-stats/helpers/calc-handicap';
import { module, test } from 'qunit';

module('Unit | Helper | calc handicap');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calcHandicap([42]);
  assert.ok(result);
});

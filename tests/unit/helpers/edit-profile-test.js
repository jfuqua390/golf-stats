import { editProfile } from 'golf-stats/helpers/edit-profile';
import { module, test } from 'qunit';

module('Unit | Helper | edit profile');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = editProfile([42]);
  assert.ok(result);
});

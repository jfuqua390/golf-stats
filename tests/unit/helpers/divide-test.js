import { divide } from 'golf-stats/helpers/divide';
import { module, test } from 'qunit';

module('Unit | Helper | divide');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = divide([42]);
  assert.ok(result);
});
